// import path from 'path';
// import express, { Express } from 'express';

// const configViewEngine = (app: Express): void => {
//     // Sử dụng process.cwd() để lấy thư mục làm việc hiện tại
//     const staticPath = path.resolve(process.cwd(), 'public');  // Đường dẫn tuyệt đối đến thư mục public

//     // Cấu hình Express để phục vụ tệp tĩnh
//     app.use(express.static(staticPath));  // Đảm bảo đường dẫn chính xác
// };

// export default configViewEngine;
